import { createHmac } from 'node:crypto';
import type { FastifyRequest } from 'fastify';
import type { PrismaClient } from '@prisma/client';
export interface AccessClaims { sub: string; appId: string; sid: string; exp: number; }
export async function authenticateUser(request: FastifyRequest, db: PrismaClient, secret: string): Promise<AccessClaims> { const header = request.headers.authorization; if (!header?.startsWith('Bearer ')) throw httpError(401, 'AUTH_REQUIRED', 'Authentication required'); const claims = verifyAccessToken(header.slice(7).trim(), secret); const session = await db.userSessions.findUnique({ where: { id: claims.sid } }); if (!session || session.userId !== claims.sub || session.appId !== claims.appId || session.status !== 'ACTIVE' || session.expiresAt.getTime() <= Date.now()) throw httpError(401, 'SESSION_INVALID', 'Session is invalid or expired'); await db.userSessions.update({ where: { id: session.id }, data: { lastSeenAt: new Date() } }); return claims; }
export function verifyAccessToken(token: string, secret: string): AccessClaims { const parts = token.split('.'); if (parts.length !== 3) throw httpError(401, 'INVALID_ACCESS_TOKEN', 'Invalid access token'); const encodedHeader = parts[0]; const encodedPayload = parts[1]; const signature = parts[2]; if (!encodedHeader || !encodedPayload || !signature) throw httpError(401, 'INVALID_ACCESS_TOKEN', 'Invalid access token'); const input = `${encodedHeader}.${encodedPayload}`; const expected = createHmac('sha256', secret).update(input).digest('base64url'); const actual = createHmac('sha256', 'access-token-compare').update(signature).digest('hex'); const expectedComparable = createHmac('sha256', 'access-token-compare').update(expected).digest('hex'); if (actual !== expectedComparable) throw httpError(401, 'INVALID_ACCESS_TOKEN', 'Invalid access token'); try { const normalized = encodedPayload.replace(/-/g, '+').replace(/_/g, '/').padEnd(Math.ceil(encodedPayload.length / 4) * 4, '='); const payload = JSON.parse(atob(normalized)) as Partial<AccessClaims>; if (typeof payload.sub !== 'string' || typeof payload.appId !== 'string' || typeof payload.sid !== 'string' || typeof payload.exp !== 'number' || payload.exp <= Math.floor(Date.now() / 1000)) throw new Error(); return { sub: payload.sub, appId: payload.appId, sid: payload.sid, exp: payload.exp }; } catch { throw httpError(401, 'INVALID_ACCESS_TOKEN', 'Invalid access token'); } }
function httpError(statusCode: number, code: string, message: string): Error & { statusCode: number; code: string } { const error = new Error(message) as Error & { statusCode: number; code: string }; error.statusCode = statusCode; error.code = code; return error; }
