# Rewzio Android

Kotlin + Jetpack Compose + MVVM/Clean Architecture foundation.

Security boundaries: provider secrets are never bundled; rewards and balances remain server-authoritative. Token storage is abstracted behind TokenStore; production implementation should use Android Keystore-backed storage.
